import { WhatWouldIDoVisitorPipe } from './what-would-i-do-visitor.pipe';

describe('WhatWouldIDoVisitorPipe', () => {
  it('create an instance', () => {
    const pipe = new WhatWouldIDoVisitorPipe();
    expect(pipe).toBeTruthy();
  });
});
